Build the TypeScript project by running the TypeScript compiler.

```bash
cd /home/user/h2568 && npm run build
```

Report any compiler errors. If the build succeeds, confirm the output was written to `dist-cjs/`.
