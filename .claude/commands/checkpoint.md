Create a new checkpoint to save the current state of your work.

Ask the user for an optional name for the checkpoint, then run:

```bash
cd /home/user/h2568 && npx . checkpoint create [name]
```

Confirm the checkpoint was created by showing the ID. Mention they can restore it later with `/reset`.
